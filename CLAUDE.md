# Claude Code Notes

When working in this repository:

- Read `AGENTS.md` first.
- Treat docs, templates, and datasets as the product.
- Use official sources for provider capabilities.
- Run `npm test` and `npm run validate:dataset` after changes.
- Keep Japanese examples safe for non-engineers.

